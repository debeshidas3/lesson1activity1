import pygame

pygame.init()
SCREEN_WIDTH,SCREEN_HIGHT = 500,500

display_surface =
pygame.display.set_mod((SCREEN_WIDTH, SCREEN_HEIGHT))
pygame.display.set_caption('adding image and background image')

background_image = pygame.transform.scale(pygame.image.load('background.png').con)
