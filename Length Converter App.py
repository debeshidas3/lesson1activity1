from tkinter import

window = Tk()

window.title("Length Converter App")
window.geometry("400x400")

def length converter():
    length = int(entry_get())
    length = inches
    return = length * centimeters

window.mainloop()